# AI KindleMint Engine - Production Dependencies Only
# These are the minimal dependencies needed to run the engine

# PDF Generation (Core functionality)
reportlab==4.1.0
Pillow==10.3.0
PyPDF2==3.0.1

# Configuration
python-dotenv==1.0.1
PyYAML==6.0.1

# Web scraping for market research
requests==2.32.4
beautifulsoup4==4.12.3
lxml==5.1.0

# Error tracking (optional but recommended)
sentry-sdk==2.32.0