# Environment variable template for Kindlemint Engine
# Copy to .env and fill real values, or store in GitHub/AWS secrets.

OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=dall-e-3
DALLE_IMAGE_SIZE=1024x1024
REPLICATE_SDXL_VERSION=google/imagen-4-fast
GEMINI_API_KEY=
GEMINI_PROJECT_ID=email-outreach-ai-460404
GOOGLE_APPLICATION_CREDENTIALS=secrets/email-outreach-ai.json
GEMINI_MODEL=gemini-pro
# --------------------------------------------------------------------------- #
# 🟠  Slack Integration
# --------------------------------------------------------------------------- #
# URL of the Incoming Webhook for the Slack channel where you want KindleMint
# Engine to post notifications (batch completion, errors, market research, …).
# Create one in Slack → Settings → Apps → Incoming Webhooks.
SLACK_WEBHOOK_URL=

# --------------------------------------------------------------------------- #
# 🟣  Sentry Monitoring
# --------------------------------------------------------------------------- #
# Your Sentry DSN – found in Sentry → Project Settings → Client Keys (DSN).
# Leave blank to disable error/performance monitoring.
SENTRY_DSN=
# Optional environment tag shown in Sentry (e.g. production, staging, local)
ENVIRONMENT=local

# --------------------------------------------------------------------------- #
# 🔑  Amazon KDP Credentials (optional – only if you run browser automation)
# --------------------------------------------------------------------------- #
KDP_EMAIL=
KDP_PASSWORD=
