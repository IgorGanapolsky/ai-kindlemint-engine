#!/bin/bash
# Claude-Flow costs wrapper
# Integrates Claude cost tracking with claude-flow command structure

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Execute the Python script with all arguments
python3 "$SCRIPT_DIR/scripts/claude_costs.py" "$@"
